apiVersion: v1
kind: Service
metadata:
  name: {{ include "javapp.fullname" . }}
  labels:
    {{- include "javapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "javapp.selectorLabels" . | nindent 4 }}
